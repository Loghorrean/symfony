{% include 'header.html.twig' %}
<div id = "wrapper">
    <div id = "page-wrapper">
        <div class = "container-fluid">
            <div class = "row">
                <div class = "col-lg-12">
                    <h1 class = "page-header">
                        Edit this book
                    </h1>
                </div>
                <div class = "col-xs-6">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
<a href="{{ path('main_page') }}">Go Back</a>
{% include 'footer.html.twig' %}